# Global User Rules

- **Access to /tmp/**: Always allow reading, writing, and executing operations on files in `/tmp/` (and `/private/tmp/`) across all projects. Do not prompt for permission when interacting with `/tmp/`.
